// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// VerifiedByColleague is an object representing the database table.
type VerifiedByColleague struct {
	ID          int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	SubjectID   int       `boil:"subject_id" json:"subject_id" toml:"subject_id" yaml:"subject_id"`
	ColleagueID null.Int  `boil:"colleague_id" json:"colleague_id,omitempty" toml:"colleague_id" yaml:"colleague_id,omitempty"`
	Sent        time.Time `boil:"sent" json:"sent" toml:"sent" yaml:"sent"`
	Response    null.Time `boil:"response" json:"response,omitempty" toml:"response" yaml:"response,omitempty"`

	R *verifiedByColleagueR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L verifiedByColleagueL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VerifiedByColleagueColumns = struct {
	ID          string
	SubjectID   string
	ColleagueID string
	Sent        string
	Response    string
}{
	ID:          "id",
	SubjectID:   "subject_id",
	ColleagueID: "colleague_id",
	Sent:        "sent",
	Response:    "response",
}

var VerifiedByColleagueTableColumns = struct {
	ID          string
	SubjectID   string
	ColleagueID string
	Sent        string
	Response    string
}{
	ID:          "verified_by_colleague.id",
	SubjectID:   "verified_by_colleague.subject_id",
	ColleagueID: "verified_by_colleague.colleague_id",
	Sent:        "verified_by_colleague.sent",
	Response:    "verified_by_colleague.response",
}

// Generated where

var VerifiedByColleagueWhere = struct {
	ID          whereHelperint
	SubjectID   whereHelperint
	ColleagueID whereHelpernull_Int
	Sent        whereHelpertime_Time
	Response    whereHelpernull_Time
}{
	ID:          whereHelperint{field: "\"alpha\".\"verified_by_colleague\".\"id\""},
	SubjectID:   whereHelperint{field: "\"alpha\".\"verified_by_colleague\".\"subject_id\""},
	ColleagueID: whereHelpernull_Int{field: "\"alpha\".\"verified_by_colleague\".\"colleague_id\""},
	Sent:        whereHelpertime_Time{field: "\"alpha\".\"verified_by_colleague\".\"sent\""},
	Response:    whereHelpernull_Time{field: "\"alpha\".\"verified_by_colleague\".\"response\""},
}

// VerifiedByColleagueRels is where relationship names are stored.
var VerifiedByColleagueRels = struct {
	Colleague string
	Subject   string
}{
	Colleague: "Colleague",
	Subject:   "Subject",
}

// verifiedByColleagueR is where relationships are stored.
type verifiedByColleagueR struct {
	Colleague *Person `boil:"Colleague" json:"Colleague" toml:"Colleague" yaml:"Colleague"`
	Subject   *Person `boil:"Subject" json:"Subject" toml:"Subject" yaml:"Subject"`
}

// NewStruct creates a new relationship struct
func (*verifiedByColleagueR) NewStruct() *verifiedByColleagueR {
	return &verifiedByColleagueR{}
}

func (r *verifiedByColleagueR) GetColleague() *Person {
	if r == nil {
		return nil
	}
	return r.Colleague
}

func (r *verifiedByColleagueR) GetSubject() *Person {
	if r == nil {
		return nil
	}
	return r.Subject
}

// verifiedByColleagueL is where Load methods for each relationship are stored.
type verifiedByColleagueL struct{}

var (
	verifiedByColleagueAllColumns            = []string{"id", "subject_id", "colleague_id", "sent", "response"}
	verifiedByColleagueColumnsWithoutDefault = []string{"subject_id"}
	verifiedByColleagueColumnsWithDefault    = []string{"id", "colleague_id", "sent", "response"}
	verifiedByColleaguePrimaryKeyColumns     = []string{"id"}
	verifiedByColleagueGeneratedColumns      = []string{"id"}
)

type (
	// VerifiedByColleagueSlice is an alias for a slice of pointers to VerifiedByColleague.
	// This should almost always be used instead of []VerifiedByColleague.
	VerifiedByColleagueSlice []*VerifiedByColleague

	verifiedByColleagueQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	verifiedByColleagueType                 = reflect.TypeOf(&VerifiedByColleague{})
	verifiedByColleagueMapping              = queries.MakeStructMapping(verifiedByColleagueType)
	verifiedByColleaguePrimaryKeyMapping, _ = queries.BindMapping(verifiedByColleagueType, verifiedByColleagueMapping, verifiedByColleaguePrimaryKeyColumns)
	verifiedByColleagueInsertCacheMut       sync.RWMutex
	verifiedByColleagueInsertCache          = make(map[string]insertCache)
	verifiedByColleagueUpdateCacheMut       sync.RWMutex
	verifiedByColleagueUpdateCache          = make(map[string]updateCache)
	verifiedByColleagueUpsertCacheMut       sync.RWMutex
	verifiedByColleagueUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single verifiedByColleague record from the query using the global executor.
func (q verifiedByColleagueQuery) OneG(ctx context.Context) (*VerifiedByColleague, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single verifiedByColleague record from the query.
func (q verifiedByColleagueQuery) One(ctx context.Context, exec boil.ContextExecutor) (*VerifiedByColleague, error) {
	o := &VerifiedByColleague{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for verified_by_colleague")
	}

	return o, nil
}

// AllG returns all VerifiedByColleague records from the query using the global executor.
func (q verifiedByColleagueQuery) AllG(ctx context.Context) (VerifiedByColleagueSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all VerifiedByColleague records from the query.
func (q verifiedByColleagueQuery) All(ctx context.Context, exec boil.ContextExecutor) (VerifiedByColleagueSlice, error) {
	var o []*VerifiedByColleague

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to VerifiedByColleague slice")
	}

	return o, nil
}

// CountG returns the count of all VerifiedByColleague records in the query using the global executor
func (q verifiedByColleagueQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all VerifiedByColleague records in the query.
func (q verifiedByColleagueQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count verified_by_colleague rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q verifiedByColleagueQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q verifiedByColleagueQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if verified_by_colleague exists")
	}

	return count > 0, nil
}

// Colleague pointed to by the foreign key.
func (o *VerifiedByColleague) Colleague(mods ...qm.QueryMod) personQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ColleagueID),
	}

	queryMods = append(queryMods, mods...)

	return Persons(queryMods...)
}

// Subject pointed to by the foreign key.
func (o *VerifiedByColleague) Subject(mods ...qm.QueryMod) personQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SubjectID),
	}

	queryMods = append(queryMods, mods...)

	return Persons(queryMods...)
}

// LoadColleague allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (verifiedByColleagueL) LoadColleague(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVerifiedByColleague interface{}, mods queries.Applicator) error {
	var slice []*VerifiedByColleague
	var object *VerifiedByColleague

	if singular {
		var ok bool
		object, ok = maybeVerifiedByColleague.(*VerifiedByColleague)
		if !ok {
			object = new(VerifiedByColleague)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVerifiedByColleague)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVerifiedByColleague))
			}
		}
	} else {
		s, ok := maybeVerifiedByColleague.(*[]*VerifiedByColleague)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVerifiedByColleague)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVerifiedByColleague))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &verifiedByColleagueR{}
		}
		if !queries.IsNil(object.ColleagueID) {
			args = append(args, object.ColleagueID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &verifiedByColleagueR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ColleagueID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.ColleagueID) {
				args = append(args, obj.ColleagueID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`alpha.persons`),
		qm.WhereIn(`alpha.persons.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Person")
	}

	var resultSlice []*Person
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Person")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for persons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for persons")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Colleague = foreign
		if foreign.R == nil {
			foreign.R = &personR{}
		}
		foreign.R.ColleagueVerifiedByColleagues = append(foreign.R.ColleagueVerifiedByColleagues, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ColleagueID, foreign.ID) {
				local.R.Colleague = foreign
				if foreign.R == nil {
					foreign.R = &personR{}
				}
				foreign.R.ColleagueVerifiedByColleagues = append(foreign.R.ColleagueVerifiedByColleagues, local)
				break
			}
		}
	}

	return nil
}

// LoadSubject allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (verifiedByColleagueL) LoadSubject(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVerifiedByColleague interface{}, mods queries.Applicator) error {
	var slice []*VerifiedByColleague
	var object *VerifiedByColleague

	if singular {
		var ok bool
		object, ok = maybeVerifiedByColleague.(*VerifiedByColleague)
		if !ok {
			object = new(VerifiedByColleague)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVerifiedByColleague)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVerifiedByColleague))
			}
		}
	} else {
		s, ok := maybeVerifiedByColleague.(*[]*VerifiedByColleague)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVerifiedByColleague)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVerifiedByColleague))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &verifiedByColleagueR{}
		}
		args = append(args, object.SubjectID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &verifiedByColleagueR{}
			}

			for _, a := range args {
				if a == obj.SubjectID {
					continue Outer
				}
			}

			args = append(args, obj.SubjectID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`alpha.persons`),
		qm.WhereIn(`alpha.persons.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Person")
	}

	var resultSlice []*Person
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Person")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for persons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for persons")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Subject = foreign
		if foreign.R == nil {
			foreign.R = &personR{}
		}
		foreign.R.SubjectVerifiedByColleagues = append(foreign.R.SubjectVerifiedByColleagues, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SubjectID == foreign.ID {
				local.R.Subject = foreign
				if foreign.R == nil {
					foreign.R = &personR{}
				}
				foreign.R.SubjectVerifiedByColleagues = append(foreign.R.SubjectVerifiedByColleagues, local)
				break
			}
		}
	}

	return nil
}

// SetColleagueG of the verifiedByColleague to the related item.
// Sets o.R.Colleague to related.
// Adds o to related.R.ColleagueVerifiedByColleagues.
// Uses the global database handle.
func (o *VerifiedByColleague) SetColleagueG(ctx context.Context, insert bool, related *Person) error {
	return o.SetColleague(ctx, boil.GetContextDB(), insert, related)
}

// SetColleague of the verifiedByColleague to the related item.
// Sets o.R.Colleague to related.
// Adds o to related.R.ColleagueVerifiedByColleagues.
func (o *VerifiedByColleague) SetColleague(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Person) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alpha\".\"verified_by_colleague\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"colleague_id"}),
		strmangle.WhereClause("\"", "\"", 2, verifiedByColleaguePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ColleagueID, related.ID)
	if o.R == nil {
		o.R = &verifiedByColleagueR{
			Colleague: related,
		}
	} else {
		o.R.Colleague = related
	}

	if related.R == nil {
		related.R = &personR{
			ColleagueVerifiedByColleagues: VerifiedByColleagueSlice{o},
		}
	} else {
		related.R.ColleagueVerifiedByColleagues = append(related.R.ColleagueVerifiedByColleagues, o)
	}

	return nil
}

// RemoveColleagueG relationship.
// Sets o.R.Colleague to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *VerifiedByColleague) RemoveColleagueG(ctx context.Context, related *Person) error {
	return o.RemoveColleague(ctx, boil.GetContextDB(), related)
}

// RemoveColleague relationship.
// Sets o.R.Colleague to nil.
// Removes o from all passed in related items' relationships struct.
func (o *VerifiedByColleague) RemoveColleague(ctx context.Context, exec boil.ContextExecutor, related *Person) error {
	var err error

	queries.SetScanner(&o.ColleagueID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("colleague_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Colleague = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ColleagueVerifiedByColleagues {
		if queries.Equal(o.ColleagueID, ri.ColleagueID) {
			continue
		}

		ln := len(related.R.ColleagueVerifiedByColleagues)
		if ln > 1 && i < ln-1 {
			related.R.ColleagueVerifiedByColleagues[i] = related.R.ColleagueVerifiedByColleagues[ln-1]
		}
		related.R.ColleagueVerifiedByColleagues = related.R.ColleagueVerifiedByColleagues[:ln-1]
		break
	}
	return nil
}

// SetSubjectG of the verifiedByColleague to the related item.
// Sets o.R.Subject to related.
// Adds o to related.R.SubjectVerifiedByColleagues.
// Uses the global database handle.
func (o *VerifiedByColleague) SetSubjectG(ctx context.Context, insert bool, related *Person) error {
	return o.SetSubject(ctx, boil.GetContextDB(), insert, related)
}

// SetSubject of the verifiedByColleague to the related item.
// Sets o.R.Subject to related.
// Adds o to related.R.SubjectVerifiedByColleagues.
func (o *VerifiedByColleague) SetSubject(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Person) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alpha\".\"verified_by_colleague\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"subject_id"}),
		strmangle.WhereClause("\"", "\"", 2, verifiedByColleaguePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SubjectID = related.ID
	if o.R == nil {
		o.R = &verifiedByColleagueR{
			Subject: related,
		}
	} else {
		o.R.Subject = related
	}

	if related.R == nil {
		related.R = &personR{
			SubjectVerifiedByColleagues: VerifiedByColleagueSlice{o},
		}
	} else {
		related.R.SubjectVerifiedByColleagues = append(related.R.SubjectVerifiedByColleagues, o)
	}

	return nil
}

// VerifiedByColleagues retrieves all the records using an executor.
func VerifiedByColleagues(mods ...qm.QueryMod) verifiedByColleagueQuery {
	mods = append(mods, qm.From("\"alpha\".\"verified_by_colleague\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"alpha\".\"verified_by_colleague\".*"})
	}

	return verifiedByColleagueQuery{q}
}

// FindVerifiedByColleagueG retrieves a single record by ID.
func FindVerifiedByColleagueG(ctx context.Context, iD int, selectCols ...string) (*VerifiedByColleague, error) {
	return FindVerifiedByColleague(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindVerifiedByColleague retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVerifiedByColleague(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*VerifiedByColleague, error) {
	verifiedByColleagueObj := &VerifiedByColleague{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"alpha\".\"verified_by_colleague\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, verifiedByColleagueObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from verified_by_colleague")
	}

	return verifiedByColleagueObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *VerifiedByColleague) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VerifiedByColleague) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no verified_by_colleague provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(verifiedByColleagueColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	verifiedByColleagueInsertCacheMut.RLock()
	cache, cached := verifiedByColleagueInsertCache[key]
	verifiedByColleagueInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			verifiedByColleagueAllColumns,
			verifiedByColleagueColumnsWithDefault,
			verifiedByColleagueColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, verifiedByColleagueGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(verifiedByColleagueType, verifiedByColleagueMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(verifiedByColleagueType, verifiedByColleagueMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"alpha\".\"verified_by_colleague\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"alpha\".\"verified_by_colleague\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into verified_by_colleague")
	}

	if !cached {
		verifiedByColleagueInsertCacheMut.Lock()
		verifiedByColleagueInsertCache[key] = cache
		verifiedByColleagueInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single VerifiedByColleague record using the global executor.
// See Update for more documentation.
func (o *VerifiedByColleague) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the VerifiedByColleague.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VerifiedByColleague) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	verifiedByColleagueUpdateCacheMut.RLock()
	cache, cached := verifiedByColleagueUpdateCache[key]
	verifiedByColleagueUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			verifiedByColleagueAllColumns,
			verifiedByColleaguePrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, verifiedByColleagueGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update verified_by_colleague, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"alpha\".\"verified_by_colleague\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, verifiedByColleaguePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(verifiedByColleagueType, verifiedByColleagueMapping, append(wl, verifiedByColleaguePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update verified_by_colleague row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for verified_by_colleague")
	}

	if !cached {
		verifiedByColleagueUpdateCacheMut.Lock()
		verifiedByColleagueUpdateCache[key] = cache
		verifiedByColleagueUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q verifiedByColleagueQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q verifiedByColleagueQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for verified_by_colleague")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for verified_by_colleague")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o VerifiedByColleagueSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VerifiedByColleagueSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), verifiedByColleaguePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"alpha\".\"verified_by_colleague\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, verifiedByColleaguePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in verifiedByColleague slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all verifiedByColleague")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *VerifiedByColleague) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VerifiedByColleague) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no verified_by_colleague provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(verifiedByColleagueColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	verifiedByColleagueUpsertCacheMut.RLock()
	cache, cached := verifiedByColleagueUpsertCache[key]
	verifiedByColleagueUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			verifiedByColleagueAllColumns,
			verifiedByColleagueColumnsWithDefault,
			verifiedByColleagueColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			verifiedByColleagueAllColumns,
			verifiedByColleaguePrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, verifiedByColleagueGeneratedColumns)
		update = strmangle.SetComplement(update, verifiedByColleagueGeneratedColumns)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert verified_by_colleague, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(verifiedByColleaguePrimaryKeyColumns))
			copy(conflict, verifiedByColleaguePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"alpha\".\"verified_by_colleague\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(verifiedByColleagueType, verifiedByColleagueMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(verifiedByColleagueType, verifiedByColleagueMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert verified_by_colleague")
	}

	if !cached {
		verifiedByColleagueUpsertCacheMut.Lock()
		verifiedByColleagueUpsertCache[key] = cache
		verifiedByColleagueUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single VerifiedByColleague record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *VerifiedByColleague) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single VerifiedByColleague record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VerifiedByColleague) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no VerifiedByColleague provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), verifiedByColleaguePrimaryKeyMapping)
	sql := "DELETE FROM \"alpha\".\"verified_by_colleague\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from verified_by_colleague")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for verified_by_colleague")
	}

	return rowsAff, nil
}

func (q verifiedByColleagueQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q verifiedByColleagueQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no verifiedByColleagueQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from verified_by_colleague")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for verified_by_colleague")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o VerifiedByColleagueSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VerifiedByColleagueSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), verifiedByColleaguePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"alpha\".\"verified_by_colleague\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, verifiedByColleaguePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from verifiedByColleague slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for verified_by_colleague")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *VerifiedByColleague) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no VerifiedByColleague provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VerifiedByColleague) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindVerifiedByColleague(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VerifiedByColleagueSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty VerifiedByColleagueSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VerifiedByColleagueSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VerifiedByColleagueSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), verifiedByColleaguePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"alpha\".\"verified_by_colleague\".* FROM \"alpha\".\"verified_by_colleague\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, verifiedByColleaguePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in VerifiedByColleagueSlice")
	}

	*o = slice

	return nil
}

// VerifiedByColleagueExistsG checks if the VerifiedByColleague row exists.
func VerifiedByColleagueExistsG(ctx context.Context, iD int) (bool, error) {
	return VerifiedByColleagueExists(ctx, boil.GetContextDB(), iD)
}

// VerifiedByColleagueExists checks if the VerifiedByColleague row exists.
func VerifiedByColleagueExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"alpha\".\"verified_by_colleague\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if verified_by_colleague exists")
	}

	return exists, nil
}
