// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Digest is an object representing the database table.
type Digest struct {
	Email  string `boil:"email" json:"email" toml:"email" yaml:"email"`
	Digest string `boil:"digest" json:"digest" toml:"digest" yaml:"digest"`

	R *digestR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L digestL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DigestColumns = struct {
	Email  string
	Digest string
}{
	Email:  "email",
	Digest: "digest",
}

var DigestTableColumns = struct {
	Email  string
	Digest string
}{
	Email:  "digests.email",
	Digest: "digests.digest",
}

// Generated where

var DigestWhere = struct {
	Email  whereHelperstring
	Digest whereHelperstring
}{
	Email:  whereHelperstring{field: "\"alpha\".\"digests\".\"email\""},
	Digest: whereHelperstring{field: "\"alpha\".\"digests\".\"digest\""},
}

// DigestRels is where relationship names are stored.
var DigestRels = struct {
}{}

// digestR is where relationships are stored.
type digestR struct {
}

// NewStruct creates a new relationship struct
func (*digestR) NewStruct() *digestR {
	return &digestR{}
}

// digestL is where Load methods for each relationship are stored.
type digestL struct{}

var (
	digestAllColumns            = []string{"email", "digest"}
	digestColumnsWithoutDefault = []string{"email", "digest"}
	digestColumnsWithDefault    = []string{}
	digestPrimaryKeyColumns     = []string{"email"}
	digestGeneratedColumns      = []string{}
)

type (
	// DigestSlice is an alias for a slice of pointers to Digest.
	// This should almost always be used instead of []Digest.
	DigestSlice []*Digest

	digestQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	digestType                 = reflect.TypeOf(&Digest{})
	digestMapping              = queries.MakeStructMapping(digestType)
	digestPrimaryKeyMapping, _ = queries.BindMapping(digestType, digestMapping, digestPrimaryKeyColumns)
	digestInsertCacheMut       sync.RWMutex
	digestInsertCache          = make(map[string]insertCache)
	digestUpdateCacheMut       sync.RWMutex
	digestUpdateCache          = make(map[string]updateCache)
	digestUpsertCacheMut       sync.RWMutex
	digestUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single digest record from the query using the global executor.
func (q digestQuery) OneG(ctx context.Context) (*Digest, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single digest record from the query.
func (q digestQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Digest, error) {
	o := &Digest{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for digests")
	}

	return o, nil
}

// AllG returns all Digest records from the query using the global executor.
func (q digestQuery) AllG(ctx context.Context) (DigestSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all Digest records from the query.
func (q digestQuery) All(ctx context.Context, exec boil.ContextExecutor) (DigestSlice, error) {
	var o []*Digest

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Digest slice")
	}

	return o, nil
}

// CountG returns the count of all Digest records in the query using the global executor
func (q digestQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all Digest records in the query.
func (q digestQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count digests rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q digestQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q digestQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if digests exists")
	}

	return count > 0, nil
}

// Digests retrieves all the records using an executor.
func Digests(mods ...qm.QueryMod) digestQuery {
	mods = append(mods, qm.From("\"alpha\".\"digests\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"alpha\".\"digests\".*"})
	}

	return digestQuery{q}
}

// FindDigestG retrieves a single record by ID.
func FindDigestG(ctx context.Context, email string, selectCols ...string) (*Digest, error) {
	return FindDigest(ctx, boil.GetContextDB(), email, selectCols...)
}

// FindDigest retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDigest(ctx context.Context, exec boil.ContextExecutor, email string, selectCols ...string) (*Digest, error) {
	digestObj := &Digest{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"alpha\".\"digests\" where \"email\"=$1", sel,
	)

	q := queries.Raw(query, email)

	err := q.Bind(ctx, exec, digestObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from digests")
	}

	return digestObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Digest) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Digest) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no digests provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(digestColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	digestInsertCacheMut.RLock()
	cache, cached := digestInsertCache[key]
	digestInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			digestAllColumns,
			digestColumnsWithDefault,
			digestColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(digestType, digestMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(digestType, digestMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"alpha\".\"digests\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"alpha\".\"digests\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into digests")
	}

	if !cached {
		digestInsertCacheMut.Lock()
		digestInsertCache[key] = cache
		digestInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single Digest record using the global executor.
// See Update for more documentation.
func (o *Digest) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the Digest.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Digest) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	digestUpdateCacheMut.RLock()
	cache, cached := digestUpdateCache[key]
	digestUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			digestAllColumns,
			digestPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update digests, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"alpha\".\"digests\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, digestPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(digestType, digestMapping, append(wl, digestPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update digests row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for digests")
	}

	if !cached {
		digestUpdateCacheMut.Lock()
		digestUpdateCache[key] = cache
		digestUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q digestQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q digestQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for digests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for digests")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o DigestSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DigestSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), digestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"alpha\".\"digests\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, digestPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in digest slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all digest")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Digest) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Digest) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no digests provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(digestColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	digestUpsertCacheMut.RLock()
	cache, cached := digestUpsertCache[key]
	digestUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			digestAllColumns,
			digestColumnsWithDefault,
			digestColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			digestAllColumns,
			digestPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert digests, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(digestPrimaryKeyColumns))
			copy(conflict, digestPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"alpha\".\"digests\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(digestType, digestMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(digestType, digestMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert digests")
	}

	if !cached {
		digestUpsertCacheMut.Lock()
		digestUpsertCache[key] = cache
		digestUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single Digest record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Digest) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single Digest record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Digest) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Digest provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), digestPrimaryKeyMapping)
	sql := "DELETE FROM \"alpha\".\"digests\" WHERE \"email\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from digests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for digests")
	}

	return rowsAff, nil
}

func (q digestQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q digestQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no digestQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from digests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for digests")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o DigestSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DigestSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), digestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"alpha\".\"digests\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, digestPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from digest slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for digests")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Digest) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no Digest provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Digest) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDigest(ctx, exec, o.Email)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DigestSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty DigestSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DigestSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DigestSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), digestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"alpha\".\"digests\".* FROM \"alpha\".\"digests\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, digestPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DigestSlice")
	}

	*o = slice

	return nil
}

// DigestExistsG checks if the Digest row exists.
func DigestExistsG(ctx context.Context, email string) (bool, error) {
	return DigestExists(ctx, boil.GetContextDB(), email)
}

// DigestExists checks if the Digest row exists.
func DigestExists(ctx context.Context, exec boil.ContextExecutor, email string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"alpha\".\"digests\" where \"email\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, email)
	}
	row := exec.QueryRowContext(ctx, sql, email)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if digests exists")
	}

	return exists, nil
}
